require('dotenv').config();

// Arquivos de inicialização do bot vão aqui.
// A lógica de criação e inicialização do client será adicionada na próxima etapa.

console.log('Estrutura do bot carregada.');
